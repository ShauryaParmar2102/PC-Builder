# PC Builder

### Description

A full-stack PC Builder web application that allows users to configure custom PC builds using dynamic components, calculate real-time build costs, and save builds locally or to a backend showcase system. The project demonstrates CRUD operations, REST API integration, and structured cost calculation logic across frontend and backend layers.

